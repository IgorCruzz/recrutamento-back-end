version: 2.1
jobs:
  Second-Job:
    docker:
      - image: alpine:3.7
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD
    steps:
      - checkout
      - run:
          name: Segunda Etapa
          command: |
            ls -al
            yarn start:dev
workflows:
  version: 2
  Example_Workflow:
    jobs:
      - First-Job

